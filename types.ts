export interface WorkItem {
  id: string;
  title: string;
  category: string;
  image: string;
  year: string;
}

export interface MandateItem {
  title: string;
  subtitle: string;
  description: string;
  features: string[];
}
